 <% if current_user.likes?(micropost) %>
    <%= form_with(model: current_user.favorites.find_by(micropost_id: micropost.id), method: :delete) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'unlikes', class: 'mx-1 btn btn-danger btn-sm' %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.favorites.build) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'likes', class: 'mx-1 btn btn-success btn-sm' %>
    <% end %>
  <% end %>
